{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // Modules
    "allowImportingTsExtensions": true,
    "types": ["node"],

    // Emit
    "noEmit": true
  }
}
